# Optional: base URL used to fetch the agent model configuration.
# Default is /api/agent which assumes the frontend is proxied through Vite or your reverse proxy.
# VITE_AGENT_API_BASE=/api/agent

# Optional: when running `pnpm dev`, forward /api/agent to the backend server.
# Set this to the http origin of the agent config server, e.g. http://localhost:8787
# VITE_AGENT_PROXY_TARGET=http://localhost:8787
